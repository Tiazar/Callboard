<div>
<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, placeholder: "f.content"%>
  </div>
  <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
<% end %>
<div>
